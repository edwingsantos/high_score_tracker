
make a function for loged in
    while loop
        if user is loged in 
            make user type their username
            make user type their password
            check if username and password are in the csvfile
                if username and password are not in the csv file, print that the account is not avalible and call main
                else make a string called chioce and ask the user if they want to pick a game or change user details
            make a while loop
            if user picks to play game, check if the game is avalible
            if game is not avalible then continue print this game does not exist 
            else call the game function
            if user picks to change user details
            open the csv file of the username, and delete the line of the user that they want to deleted
            make user put their new username and password
            make user select an user name
            call user registration function
        else print to create an account and call main
make a function for not loged in
    while loop 
        ask user to login, create a new account or log out
        if user picks to 


make a function called main
make a while loop
Ask user if they are loged in or if they want to exit
