make a funtion for game chioce
    make a while loop
         make a string called chioce and ask the user if they want to pick a game or change user details
            if user picks to play game, check if the game is avalible
            if game is not avalible then continue print this game does not exist 
            else call the game function
            if user picks to change user details
            open the csv file of the username, and delete the line of the user that they want to deleted
            make user put their new username and password
            make user select an user name
            call user registration function
        else print to create an account and call main

make a function for not loged in
    while loop 
        ask user to login, create a new account or log out
        if user picks to create an account, call the registration funtion
        elif user picks to log in/ log out, call that funtion funtion
        else print to select an actual option and continue

make a function for loged in
    while loop
        if user is loged in 
            make user type their username
            make user type their password
            open the csv file and read it as file
            check if username and password are in the file
                if username and password are not in the csv file, print that the account is not avalible and return
                elif ask the user if they want to change account detail, play a game of log out
                        if user choice is pick game call the game funtion
                        elif user choice is to 
                        elif choice is to log out, call main
                 
make a funtion for game choice
    make a while loop
        if game is flapy bird, call flappy bird 
        elif game is pong, call pong, 
         elif game is reaction time, call reaction time 
        else print to select a actual option and continue

make a funtion for change detail
    open the csv file and write it as file 
    make user select the line they want to delete
        delete the name and password of the csv file
    make user input their new name and password 
    call user registration funtion